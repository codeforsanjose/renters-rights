import ReactOnRails from 'react-on-rails';

import RentersFriend from '../components/Renters';

// This is how react_on_rails can see the components in the browser.
ReactOnRails.register({
  RentersFriends,
});
